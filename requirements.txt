# AIROS Opportunity OS v1.0 — Dependencies

# HTTP client
httpx==0.27.0

# Telegram bot
python-telegram-bot==20.7

# Supabase
supabase==2.3.4

# PDF generation
reportlab==4.1.0

# PDF parsing (CV import)
pypdf==4.2.0
pdfminer.six==20231228

# DOCX parsing (CV import)
python-docx==1.1.0

# Browser automation (Browserless via HTTP — no local install needed)
# httpx handles all Browserless API calls

# HTML parsing (fallback text extraction)
beautifulsoup4==4.12.3
lxml==5.1.0

# Scheduling (v2 ready — not used in v1 but included for upgrade path)
# APScheduler==3.10.4

# Utilities
python-dotenv==1.0.1
pytz==2024.1
